---
name: sync-ai-spec
description: |
  manyak-ai 구현 변경을 knk-harness의 AI 서버 문서 두 개에 반영하는 스킬.
  제품 스펙(docs/product-specs/5-1-ai-server-spec.md)에는 현재 구현만 짧게 적고,
  의사결정 기록(docs/product-specs/5-2-ai-server-adr.md)에는 문제·이유·방법·선택 근거를
  4층 서술로 남긴다. 스펙 메타 표의 "기준 코드 SHA" 이후 dev에 쌓인 변경을 감지하고,
  변경마다 스펙만 고칠지 ADR까지 고칠지 가른 뒤 갱신한다.

  다음 상황에서 반드시 사용하라:
  - "스펙 동기화", "5-1 스펙 갱신", "ai 스펙 업데이트", "ADR 반영" 등의 요청
    (옛 이름 "5-ai-server 갱신"도 같은 뜻이다)
  - manyak-ai 구현 변경(PR)이 dev에 머지된 뒤 스펙 반영 티켓을 진행할 때
  - "스펙이랑 구현이 맞는지 확인해줘" 같은 드리프트 점검 요청
  - 릴리스 전 스펙-구현 일치 확인 요청
---

# AI 서버 스펙 동기화 (sync-ai-spec)

knk-harness의 AI 서버 문서는 둘이다. 구현이 바뀌면 둘 다 따라 바뀌어야 하는데, 사람 기억에
맡기면 조용히 어긋난다(드리프트). 이 스킬은 그 어긋남을 git으로 감지하고, 두 문서의 역할에 맞게 갱신한다.

| 문서 | 담는 것 | 쓰는 법 |
| --- | --- | --- |
| `docs/product-specs/5-1-ai-server-spec.md` (스펙) | **현재 구현**의 계약·값·흐름 | 지금 어떻게 동작하는지만 짧게. 과거에 어땠는지, 왜 바꿨는지는 적지 않고 지운다 |
| `docs/product-specs/5-2-ai-server-adr.md` (ADR) | 설계 결정의 **문제·이유·방법·선택 근거** | 무엇 → 왜 → 어떻게 → 왜 그 방법의 4층 서술. 폐기한 결정도 지우지 않고 대체 이유를 남긴다 |

## 작업 방식

- 스펙 반영은 **작업 티켓과 별도의 티켓**으로 한다. 작업 티켓을 만들 때 Phase별
  "스펙 문서 업데이트" 티켓(예: KNK-1125 "Phase 3 스펙 문서 업데이트") 아래에 자식 티켓을 함께
  만들어 두고, 구현이 `dev`에 머지되면 그 자식 티켓으로 이 스킬을 돌린다. 자식 티켓을 만드는
  규칙은 `CLAUDE.md` 작업 워크플로가 정본이다.
- 자식 티켓이 아직 없으면 **만들 내용(제목·부모·담당자·추정치)을 먼저 보여주고 승인받은 뒤** 만든다 → §5
- 하네스 브랜치·커밋·PR 제목은 모두 그 자식 티켓 키를 쓴다(선례: `[KNK-689] Docs: …`,
  브랜치 `docs/KNK-689-…`).

## 같이 쓰는 파일

경로는 레포 루트 기준이다.

- `.agents/skills/sync-ai-spec/scripts/detect-drift.sh` — 드리프트 감지. **읽지 말고 실행한다.**
  종료코드가 판정이고, 감시 경로와 기준 SHA를 스스로 읽는다(손으로 옮겨 적지 않는다).
- `.agents/skills/sync-ai-spec/reference.md` — 변경 분류(§1), ADR 4층 서술과 D 카탈로그 규칙(§2),
  갱신 제안·문체(§3), 종료코드 대응(§4), 티켓·커밋·PR(§5), 금지 사항(§6), 완료 보고 형식(§7).

## 하드 룰

- **수정 대상은 `5-1-ai-server-spec.md`와 `5-2-ai-server-adr.md` 둘뿐이다.** 하네스의 다른 문서는
  각자 소유자가 있다 — 불일치를 발견해도 고치지 말고 보고만 한다.
- **스펙에는 현재만 남긴다.** 바뀐 값·계약은 새 값으로 덮어쓰고, "이전에는 …였다"류 서술은 지운다.
  근거가 필요하면 ADR 항목으로 연결만 한다.
- **결정이 걸리면 ADR부터 쓴다.** 방법을 갈아탄 변경을 스펙 값만 고쳐 끝내면 ADR의 근거 서술이
  거짓이 된다. 어느 변경이 결정인지 가르는 기준 → §1
- **확인 후 반영한다.** 갱신안(어느 문서·어느 항목·요지)을 먼저 보여주고 승인받은 뒤 고친다.
- **변경과 무관한 곳은 재작성하지 않는다.** diff가 가리키는 항목만 수정한다.
- **모호하면 질문한다.** 근거가 부족한데 "값만 바뀜"으로 단정하지 않는다. 없던 대안 비교를 지어내지도 않는다.
- **압축된 말을 쓰지 않는다.** 일상에서 안 쓰는 짧은 한자말로 뜻을 뭉뚱그리지 않는다 — 금지 목록과
  대체어는 §3에 있다. 문서 본문뿐 아니라 커밋 메시지·PR 본문·사용자 보고에도 적용된다.
- 나머지 금지 사항 → §6

## 1단계 — 감지

- [ ] `bash .agents/skills/sync-ai-spec/scripts/detect-drift.sh`
- [ ] 종료코드로 갈린다 → 상세는 `reference.md` §4
      - `0` — 변경 없음. "스펙은 최신입니다"로 보고하고 끝낸다
      - `10` — 감시 경로 안에 변경 있음. 커밋(또는 머지 PR) 단위로 묶어 2단계로
      - `11` — 감시 경로 **밖** 변경만 있음. 끝내지 말고 그 커밋 메시지를 읽어,
        설계 변경을 시사하면 2단계로, 아니면 "스펙 최신"으로 보고한다
      - `1`·`3`·`4`·`5` — 스펙 파일 없음 / 기준 SHA 없음 / 기준 SHA가 로컬에 없음 /
        fetch 실패. §4대로 대응한다. **실패를 "최신"으로 보고하지 않는다**

## 2단계 — 변경 분류 (변경 단위마다)

- [ ] 커밋 메시지·PR 본문·`spec/` 동반 변경을 함께 읽고, **값·계약만 바뀌었는지, 결정이 바뀌었는지** 가른다 → §1
      - 값·계약만 → 스펙의 해당 절만 고친다
      - 결정 → ADR에 4층으로 쓰고(§2), 스펙에는 결과만 반영한다

## 3단계 — 제안과 승인

- [ ] 갱신안을 문서·항목·요지로 보여주고 **승인받는다** → §3
- [ ] 스펙 반영 자식 티켓이 없으면 만들 내용을 함께 보여주고 승인받는다 → §5

## 4단계 — 갱신

- [ ] 승인받은 항목만 수정한다. 스펙은 현재 상태만·짧게, ADR은 4층 순서·기존 문체·LF 유지 → §3
- [ ] 메타 갱신 — 스펙: 기준 코드 SHA(스크립트가 출력한 12자리)·표와 frontmatter의 버전·frontmatter `updated`.
      ADR을 고쳤으면 ADR의 버전·`updated`도 올린다

## 5단계 — 커밋·PR

- [ ] knk-harness에서 pull 후 자식 티켓 키로 분기 → 공용 스킬로 커밋·Draft PR → §5

## 완료 보고

`reference.md` §7 형식을 쓴다 — 기준 코드 변화 / 변경 단위 수와 분류 / 수정한 문서·항목 /
제약 표 변동 / 다음 단계.
