---
name: daily-sentry-digest
description: 지난 24시간 Sentry 오류를 세 서비스(ai·server·web) 통틀어 한 통으로 묶어 팀 슬랙 채널에 올릴 때 사용합니다. 매일 오전 10시 KST 클라우드 루틴이 이 스킬을 호출하고, 사람이 "센트리 요약 보내줘", "어제 오류 정리해서 슬랙에 올려줘", "오늘 오류 다이제스트"라고 요청할 때도 씁니다. 오류 하나를 골라 제보하는 것은 send-slack-report를 쓰고, 이 스킬은 하루치 전체를 요약하는 정기 보고 전용입니다.
---

# 센트리 일일 요약 (daily-sentry-digest)

## 목적

지난 24시간 Sentry 오류를 세 서비스 통틀어 읽을 수 있는 한 통으로 만들어 슬랙에 올린다.
핵심은 전부 옮기는 것이 아니라 **덜 옮기는 것**이다. 그냥 나열하면 웹의 반복 오류가
백엔드 오류를 덮어 아무도 안 읽는다.

KNK-711. 규칙의 근거·결정 이력·함정은 `reference.md`에 있다.

## 같이 쓰는 파일

- `.agents/skills/daily-sentry-digest/reference.md` — 조회·분류·메시지 규칙의 정본.
  **작성 전에 반드시 읽는다.** 임계값과 문구를 여기서 가져오고, 기억으로 대신하지 않는다.

## 필요한 것

- Sentry MCP (org `manyak`, 프로젝트 `python-fastapi`·`java-spring-boot`·`typescript-nextjs`)
- Slack MCP (전송 대상 채널은 `reference.md`의 "전송 대상" 절)

둘 중 하나라도 없으면 **작성하지 말고 멈춘다.** 조회를 못 한 채 "오류 없음"을 보내면
장애가 난 날에도 조용한 날로 보고하게 된다 — 이 파이프라인의 가장 나쁜 실패다.

## 작업 흐름

1. `reference.md`를 읽는다. 전송 대상 채널이 미정이면 여기서 멈추고 사용자에게 묻는다.
2. **이어지는 오류**를 조회한다.
   `is:unresolved environment:[prod, production]`, 창 24시간, 3개 프로젝트.
3. **새 오류**를 따로 조회한다. 위 쿼리에 `firstSeen:>{24시간 전 UTC ISO}`를 더한다.
   - 응답의 `First seen` 표시값으로 판단하지 않는다(조회 창에 따라 값이 달라진다. 함정 1).
   - `-24h` 같은 상대 기간을 쓰지 않는다. 조용히 0건을 내거나 400으로 죽는다(함정 4).
   - **0건이 나오면 7일 대조군을 한 번 더 던진다.** 대조군까지 0이면 조회가 깨진 것이므로
     "신규 없음"이 아니라 "신규 판정 실패"로 적는다(`reference.md` "0건 판정").
4. `reference.md`의 분류 규칙으로 묶는다. 백엔드는 전부 개별, 웹 끊김류는 한 줄로 접는다.
5. 본문과 스레드를 나눠 작성한다. 형식은 `reference.md`의 "메시지 형식".
   - 링크는 **조회 결과에서 복사한 URL만** 쓴다. 패턴을 조합해 만들지 않는다.
   - 확인하지 못한 인과관계는 단정하지 않고 "미확인"으로 남긴다.
6. 보낸다. 본문 전송 후, 응답의 `ts`를 `thread_ts`로 써서 스레드를 잇는다.
   - **사람이 대화 중에 부른 경우**: 전송 전에 메시지 전문을 보여 주고 확인을 받는다.
   - **클라우드 루틴이 부른 경우**: 확인 없이 보낸다. 물어볼 사람이 없다.
7. **보낸 것을 다시 읽어 확인한다.** `slack_read_thread`로 본문과 스레드를 읽고,
   빠진 내용이 없는지 본다. 전송은 성공으로 끝나도 내용 일부가 조용히 삭제될 수 있다
   (표가 그렇다. `reference.md` 함정 5). 빠졌으면 그 부분만 다시 보낸다.
8. 보낸 채널과 메시지 링크를 보고한다.

## 오류가 0건인 날

침묵하지 않는다. "지난 24시간 오류 없음" 한 줄을 보낸다.
보내지 않으면 파이프라인이 죽은 날과 조용한 날을 구분할 수 없다(사용자 결정).

## 금지 사항

- 조회에 실패한 채로 메시지를 보내지 않는다. 실패는 실패로 보고한다.
- 확인하지 못한 원인·연결을 사실처럼 적지 않는다.
- 앵커 URL을 패턴 조합으로 만들지 않는다.
- 사용자 입력 원문·채팅 원문·secret을 싣지 않는다. 식별자로 대신한다.
- 이슈 상태를 바꾸지 않는다(resolve·ignore·assign). 이 스킬은 읽고 보내기만 한다.
- `reference.md`의 임계값을 그 자리에서 바꾸지 않는다. 바꿀 일이 보이면 보고만 한다.

## 완료 보고

- 보낸 채널과 메시지 링크.
- 몇 건을 몇 줄로 줄였는지.
- 접거나 뺀 것이 있으면 무엇을 뺐는지. 조용히 줄이지 않는다.
- 조회하지 못한 프로젝트가 있으면 명시한다.
