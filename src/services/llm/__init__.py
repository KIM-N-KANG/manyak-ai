"""LLM 공통 통로 — 모델 다양성에 유연한 호출 구조(KNK-667).

호출부는 "무엇을 원하는지"만 넘기고, 모델별 차이(어느 회사 SDK인지·어떤 인자를 받는지)는
아래 두 층이 흡수한다. 그래서 모델을 바꿀 때 호출부를 고치지 않는다.

- `base.py`     — 요청·결과·스트림 이벤트·공급자 중립 예외 (층 사이의 공용 타입)
- `registry.py` — 모델 등록부. 모델 이름 → 공급자·어댑터·받는 인자 (모델 단위로 적는다)
- 어댑터        — 회사별 SDK 문법 담당. `openai_sdk.py`(DeepSeek·GPT 공용)는 KNK-671,
                  `anthropic_sdk.py`는 KNK-675에서 추가한다.

공개 함수 `complete()`/`stream()`은 어댑터가 들어오는 KNK-671에서 이 모듈에 붙는다.
"""
