# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
dist/
build/
*.egg-info/

# Env
.env
.env.jira
.env.bak-*

# Test / coverage
.pytest_cache/
.coverage
htmlcov/

# IDE
.idea/
.vscode/

# OS
.DS_Store
Thumbs.db

# Logs
*.log
*.log.err

# Claude local settings
.claude/settings.local.json

# 로컬 프롬프트 미리보기/실험 스크립트 (테스트 러너만 예외로 커밋)
scripts/*
!scripts/test.sh
!scripts/test.ps1

# AI 실험 환경 (API 키·실험 프롬프트·데이터·결과 — 전체 비공개)
experiment/

# 참고·원천 자료 창고 (정제되지 않은 크롤·발췌 등 — 로컬 전용)
references/

# 로컬 전용 스킬 (로컬 도구 scripts/·산출물 references/에만 의존 — genre-lorebook)
.agents/skills/genre-lorebook/
.claude/skills/genre-lorebook

# 로컬 전용 스킬 (AWS 계정·역할 ARN·EC2 등 인프라 식별자를 담아 공개 레포에 올리지 않음 — release-deploy)
.agents/skills/release-deploy/
.claude/skills/release-deploy

# 로컬 전용 스킬 (형제 레포 knk-harness의 내부 스펙 구조에 의존 — sync-ai-spec)
.agents/skills/sync-ai-spec/
.claude/skills/sync-ai-spec

# 로컬 전용 스킬 (팀원 실명·팀 Jira 워크플로 상태 이름을 담아 공개 레포에 올리지 않음 — create-jira-subtasks)
.agents/skills/create-jira-subtasks/
.claude/skills/create-jira-subtasks
